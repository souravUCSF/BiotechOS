.DS_Store

# Raw data exports — large, regenerable; never commit
data/raw/
*.csv.gz

# Processed/curated demo tables are committed from data/curated/ (keep those small)

# Local state + compute cache
data/biotechos.db
data/cache/

# Anonymization maps (real_InChIKey→surrogate, target, vendor) — SECRET.
# NOTE: data/corpus/ (anonymized emails+attachments) IS committed & safe to sync;
# the raw archive lives OUTSIDE the repo (~/DataStore) and is never here.
data/corpus_maps/

# Python
backend/.venv/
__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/.next/
.claude/settings.local.json

# Local secrets — never commit
backend/secrets.env
.env
