"""BiotechOS evaluation harness — regression evals for the fuzzy components
(QueryOS Q&A, extraction/classification, identity resolution, decisions).

Cases live in data/evals/<suite>.jsonl (committed). Run:
    uv run python -m biotechos.evals run [suite ...]
"""
